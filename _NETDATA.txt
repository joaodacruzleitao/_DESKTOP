   - the daemon     at /usr/sbin/netdata
   - config files   in /etc/netdata
   - web files      in /usr/share/netdata
   - plugins        in /usr/libexec/netdata
   - cache files    in /var/cache/netdata
   - db files       in /var/lib/netdata
   - log files      in /var/log/netdata
   - pid file       at /var/run/netdata.pid
   - logrotate file at /etc/logrotate.d/netdata

INSTALL
====================================================
bash <(curl -Ss https://my-netdata.io/kickstart.sh)

##############################yum install git --disableexcludes=main --skip-broken
yum -y install iproute autoconf automake curl gcc libmnl-devel libuuid-devel lm-sensors make nc nmap-ncat pkgconfig python python-psycopg2 PyYAML zlib-devel python-pip
yum install MySQL-python --disableexcludes=all

To stop netdata run:
killall netdata

To start netdata run:
/usr/sbin/netdata
chmod 755 /etc/init.d/netdata; service netdata restart;
chkconfig netdata on;

SLACK_WEBHOOK_URL="https://hooks.slack.com/services/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"

/usr/local/cpanel/scripts/builddovecotconf; /scripts/restartsrv dovecot;
service exim restart;
systemctl restart netdata || service netdata restart

PARA TESTAR CHARTS
===============================
su -s /bin/bash netdata
/usr/libexec/netdata/plugins.d/python.d.plugin 1 debug trace exim


137,138,139,

ETHTOOL_OPTS="autoneg off speed 1000 duplex full"
ethtool -G enp5s0f0 rx 2048 tx 2048
ethtool -G enp5s0f0 rx 4096
ethtool -L enp5s0f0 rx 8
ethtool -x enp5s0f0

apt install python-pip;
pip install PyMySQL;

Aumentar o max_allowed_packet em /etc/mysql/my.cnf
max_allowed_packet = 512M
wait_timeout=100


SET PASSWORD FOR 'netdata'@'localhost' = PASSWORD('Vertigo1...');
FLUSH PRIVILEGES;





TYPE=Ethernet
PROXY_METHOD=none
BROWSER_ONLY=no
BOOTPROTO=dhcp
DEFROUTE=yes
IPV4_FAILURE_FATAL=no
IPV6INIT=yes
IPV6_AUTOCONF=yes
IPV6_DEFROUTE=yes
IPV6_FAILURE_FATAL=no
IPV6_ADDR_GEN_MODE=stable-privacy
NAME=enp5s0f1
UUID=1d4746c0-77ee-49ad-8d79-5387feacbf95
DEVICE=enp5s0f1
ONBOOT=no


TYPE=Ethernet
PROXY_METHOD=none
BROWSER_ONLY=no
BOOTPROTO=none
DEFROUTE=yes
IPV4_FAILURE_FATAL=no
IPV6INIT=yes
IPV6_AUTOCONF=yes
IPV6_DEFROUTE=yes
IPV6_FAILURE_FATAL=no
IPV6_ADDR_GEN_MODE=stable-privacy
NAME=enp5s0f0
UUID=ae4267c9-ef67-4880-88c8-96b9ca2e114b
DEVICE=enp5s0f0
ONBOOT=yes
IPADDR=195.8.59.30
PREFIX=24
GATEWAY=195.8.59.254
DNS1=194.38.131.19
DNS2=195.245.176.19
DNS3=8.8.8.8
DOMAIN="hocnet.org hocnet.pt"
IPV6_PRIVACY=no
NM_CONTROLLED=no

/usr/sbin/fireqos start

FIREQOS INSTALL
===============================================
curl 'https://raw.githubusercontent.com/firehol/netdata-demo-site/master/install-all-firehol.sh' >install-all-firehol.sh
bash ./install-all-firehol.sh

/usr/sbin/ethtool -K enp0s31f6 tso off gso off gro off
echo "/usr/sbin/ethtool -K enp0s31f6 tso off gso off gro off" >> /etc/rc.local
echo "/usr/sbin/fireqos start" >> /etc/rc.local
